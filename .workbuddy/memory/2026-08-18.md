# 2026-08-18

### 14:40 CSP 第1章知识点内容上线（本地）

- **来源**：`NAS_BACKUP/.../01理论知识/1.计算机与网络基础知识.docx`（359 段落、21 张图）
- **提取方式**：python-docx 解析 runs，保留加粗/红色(#FF0000)/上标格式 → `/tmp/convert_ch1.py` 生成 HTML → 后处理把 C++ 代码段转 `<pre><code>`
- **内容文件**：`src/data/csp-content/1-knowledge.html`（后续章节按 `{id}-knowledge.html` / `{id}-practice.html` 命名放入同目录即可自动加载）
- **图片**：`public/images/csp/1/`（21 张，命名 img-01~img-21.png）
- **页面改造**：`src/pages/csp/[id]/[type].astro` 构建时用 `fs.existsSync` + `process.cwd()` 定位内容文件，`set:html` 注入；注意 `import.meta.url` 在 Vite 构建时被重写不可用
- **样式**：全局 `<style is:global>` 的 `.csp-content` 排版（h2/h3/h4、figure 单图/双图网格、quiz-block 真题黄底块、深色代码块），红色标注原样保留 inline style
- **构建验证**：26 页全部通过；第 1 章有内容，其余章节仍显示占位符
- **格式保留原则**（用户要求）：图片、文字标红、黑体加粗需与原 docx 一致

## 第 2 章《算法知识》时间复杂度内容导入
- 源文件：`~/codex/work/信息学奥赛初赛/knowledge/时间复杂度.md`（Typora 导出 HTML 太复杂，改用 md 源转换）
- 转换脚本：`/tmp/convert_ch2.py`（python-markdown + tables/fenced_code 扩展，装在 workbuddy venv）
- 产出：`src/data/csp-content/2-knowledge.html`（8 大节：复杂度基础/时间/空间/速查表/数据范围/易错点/技巧/真题 5 道）
- 图片：big-o.png、on.png → `public/images/csp/2/`
- `[type].astro` 新增样式：ul/ol 列表、blockquote（绿色 tip 块）、table（横向滚动条纹表）、行内 code（浅灰底玫红字）
- 注意：dev server 的 vite 依赖重优化会被 WorkBuddy safe-delete guard 拦截，改用 `npx astro preview` 预览 dist

## 代码块改为浅色 + highlight.js 语法高亮
- 资源放本地 `public/vendor/hljs/`：highlight.min.js (23KB) + cpp.min.js (6KB) + atom-one-light.min.css (856B)
- 解决国内访问稳定性 + 不依赖 CDN
- `[type].astro` 客户端脚本：
  - 加载 hljs 后高亮所有 `.csp-content pre code`
  - 把 `language-c++` / `language-c` 统一映射为 `language-cpp`（hljs core + 语言包模式不识别 c++ 别名）
- `.csp-content pre` 样式：浅灰底 #fafafa + 边框 + 投影（与 atom-one-light 配色协调）
- `pre code.hljs` 覆盖为 transparent 背景，避免 hljs 内部 .hljs 背景与 .csp-content pre 冲突

## 第 2 章末尾「牛油果零壹信奥🥑」推广水印清理
- 水印来源是 md 源文件尾部内嵌的两层嵌套 div（line 420-427）+ 转换产物对应位置
- 同时清理：`~/codex/work/信息学奥赛初赛/knowledge/时间复杂度.md` 与 `src/data/csp-content/2-knowledge.html`
- 经验：今后 md 源里若有「时间复杂度.html」类似课程课件，转换后要检查尾部是否夹带原文档水印
