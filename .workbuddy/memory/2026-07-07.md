# 2026-07-07 工作日志

## 23:28 SSL 证书部署 + HTTPS 启用

### 背景
- 域名 www.avocadozero.cn 已申请 SSL 证书，DNS 已解析到 47.101.55.91
- 证书文件位置：`/Users/jessechiu/NAS_BACKUP/SynologyDrive/work/avocado/公司注册/网站信息/25950128_www.avocadozero.cn_nginx/`
- 包含 `.pem`（证书）和 `.key`（私钥）两个文件

### 操作步骤
1. **上传证书到服务器**：scp 到 /tmp → sudo mv 到 `/etc/nginx/ssl/`
   - 权限：`.pem` 644，`.key` 600
2. **更新 Nginx 配置** (`server/nginx-avocado.conf`)：
   - 新增 443 SSL server block（listen 443 ssl http2）
   - 80 端口改为 301 永久跳转到 HTTPS
   - SSL 优化：TLSv1.2/v1.3，session cache 10m，session timeout 1d
   - 新增 HSTS 安全头（Strict-Transport-Security）
   - 修复 `http2 on;` 不兼容问题（改为 `listen 443 ssl http2;`）
   - 修复 gzip duplicate MIME type 警告
3. **更新 astro.config.mjs**：aliyun 默认 site 从 `http://47.101.55.91` 改为 `https://www.avocadozero.cn`
4. **更新 deploy.sh**：默认 SSH_USER 从 root 改为 admin
5. **验证**：
   - `curl -sI https://www.avocadozero.cn` → HTTP/2 200 ✅
   - `curl -sI http://www.avocadozero.cn` → 301 Moved Permanently ✅
6. **更新 push-aliyun skill**：site URL、SSH_USER、HTTPS 相关说明

### Git
- commit 6e8d558: feat: 启用 HTTPS (www.avocadozero.cn) + SSL 证书配置
- 已推送到 aliyun-deploy 分支

### 证书续期提醒
- 当前证书有效期通常 1 年，到期前需重新申请并替换 `/etc/nginx/ssl/` 下的文件
- 阿里云可在 SSL 证书控制台申请免费证书（每年 20 个免费额度）
